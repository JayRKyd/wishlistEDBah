import LandingPage from '@/components/LandingPage'

export default function RootPage() {
  // Temporarily simplified to just show landing page
  // TODO: Add auth logic back after environment variables are configured
  return <LandingPage />
}